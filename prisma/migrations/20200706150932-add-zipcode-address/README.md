# Migration `20200706150932-add-zipcode-address`

This migration has been generated by Gabriel Barreto at 7/6/2020, 3:09:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"id" SERIAL,"name_user" text  NOT NULL ,"password" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Client" (
"contact" text   ,"cpf" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text   ,"id" SERIAL,"name_client" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Address" (
"city" text   ,"client_id" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"neighborhood" text   ,"number" integer   ,"state" text   ,"street" text   ,"updatedAt" timestamp(3)  NOT NULL ,"zipcode" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Product" (
"category_id" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"name_product" text  NOT NULL ,"price" Decimal(65,30)  NOT NULL ,"price_total" Decimal(65,30)  NOT NULL ,"qtd" integer  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Category" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"name_category" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Client.cpf" ON "public"."Client"("cpf")

CREATE UNIQUE INDEX "Address_client_id" ON "public"."Address"("client_id")

ALTER TABLE "public"."Address" ADD FOREIGN KEY ("client_id")REFERENCES "public"."Client"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Product" ADD FOREIGN KEY ("category_id")REFERENCES "public"."Category"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200625194731-add-field-name-user-in-table-user..20200706150932-add-zipcode-address
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -31,8 +31,9 @@
 }
 model Address {
   id           Int      @id @default(autoincrement())
+  zipcode      Int?
   state        String?
   city         String?
   neighborhood String?
   street       String?
```


