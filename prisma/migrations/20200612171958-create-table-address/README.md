# Migration `20200612171958-create-table-address`

This migration has been generated by Gabriel Barreto at 6/12/2020, 5:19:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"email" text  NOT NULL ,"id" SERIAL,"password" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Client" (
"cpf" text   ,"email" text   ,"id" SERIAL,"name" text  NOT NULL ,"number" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Address" (
"city" text  NOT NULL ,"client_id" integer   ,"id" SERIAL,"neighborhood" text  NOT NULL ,"number" integer  NOT NULL ,"state" text  NOT NULL ,"street" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Client.cpf" ON "public"."Client"("cpf")

CREATE UNIQUE INDEX "Address_client_id" ON "public"."Address"("client_id")

ALTER TABLE "public"."Address" ADD FOREIGN KEY ("client_id")REFERENCES "public"."Client"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200612170700-create-table-clients..20200612171958-create-table-address
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url      = "postgresql://postgres:postgres@localhost:5432/prisma-crud?schema=public"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -16,10 +16,22 @@
   password String
 }
 model Client {
-  id     Int     @id @default(autoincrement())
-  name   String
-  cpf    String? @unique
-  email  String?
-  number Int
+  id      Int      @id @default(autoincrement())
+  name    String
+  cpf     String?  @unique
+  email   String?
+  number  Int
+  address Address?
 }
+
+model Address {
+  id           Int     @id @default(autoincrement())
+  state        String
+  city         String
+  neighborhood String
+  street       String
+  number       Int
+  client_id    Int?
+  client       Client? @relation(fields: [client_id], references: [id])
+}
```


