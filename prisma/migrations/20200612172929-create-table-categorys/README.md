# Migration `20200612172929-create-table-categorys`

This migration has been generated by Gabriel Barreto at 6/12/2020, 5:29:29 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"email" text  NOT NULL ,"id" SERIAL,"password" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Client" (
"cpf" text   ,"email" text   ,"id" SERIAL,"name" text  NOT NULL ,"number" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Address" (
"city" text  NOT NULL ,"client_id" integer   ,"id" SERIAL,"neighborhood" text  NOT NULL ,"number" integer  NOT NULL ,"state" text  NOT NULL ,"street" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Product" (
"id" SERIAL,"name" text  NOT NULL ,"price" Decimal(65,30)  NOT NULL ,"price_total" Decimal(65,30)  NOT NULL ,"qtd" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Category" (
"id" SERIAL,"name" text  NOT NULL ,"product_id" integer   ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Client.cpf" ON "public"."Client"("cpf")

CREATE UNIQUE INDEX "Address_client_id" ON "public"."Address"("client_id")

CREATE UNIQUE INDEX "Category_product_id" ON "public"."Category"("product_id")

ALTER TABLE "public"."Address" ADD FOREIGN KEY ("client_id")REFERENCES "public"."Client"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Category" ADD FOREIGN KEY ("product_id")REFERENCES "public"."Product"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200612172536-create-table-products..20200612172929-create-table-categorys
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url      = "postgresql://postgres:postgres@localhost:5432/prisma-crud?schema=public"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -36,10 +36,18 @@
   client       Client? @relation(fields: [client_id], references: [id])
 }
 model Product {
-  id          Int    @id @default(autoincrement())
+  id          Int      @id @default(autoincrement())
   name        String
   qtd         Int
   price       Float
   price_total Float
+  category    Category
 }
+
+model Category {
+  id         Int      @id @default(autoincrement())
+  name       String
+  product_id Int?
+  product    Product? @relation(fields: [product_id], references: [id])
+}
```


