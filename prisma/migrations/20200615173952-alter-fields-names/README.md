# Migration `20200615173952-alter-fields-names`

This migration has been generated by Gabriel Barreto at 6/15/2020, 5:39:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Category" DROP COLUMN "name",
ADD COLUMN "name_category" text  NOT NULL ;

ALTER TABLE "public"."Client" DROP COLUMN "name",
ADD COLUMN "name_client" text  NOT NULL ;

ALTER TABLE "public"."Product" DROP COLUMN "name",
ADD COLUMN "name_product" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200615172306-alter-table-product-and-category..20200615173952-alter-fields-names
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url      = "postgresql://postgres:postgres@localhost:5432/prisma-crud?schema=public"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -16,14 +16,14 @@
   password String
 }
 model Client {
-  id      Int      @id @default(autoincrement())
-  name    String
-  cpf     String?  @unique
-  email   String?
-  contact String?
-  address Address?
+  id          Int      @id @default(autoincrement())
+  name_client String
+  cpf         String?  @unique
+  email       String?
+  contact     String?
+  address     Address?
 }
 model Address {
   id           Int     @id @default(autoincrement())
@@ -36,18 +36,18 @@
   client       Client? @relation(fields: [client_id], references: [id])
 }
 model Product {
-  id          Int       @id @default(autoincrement())
-  name        String
-  qtd         Int
-  price       Float
-  price_total Float
-  category_id Int?
-  category    Category? @relation(fields: [category_id], references: [id])
+  id           Int       @id @default(autoincrement())
+  name_product String
+  qtd          Int
+  price        Float
+  price_total  Float
+  category_id  Int?
+  category     Category? @relation(fields: [category_id], references: [id])
 }
 model Category {
-  id      Int      @id @default(autoincrement())
-  name    String
-  product Product?
+  id            Int      @id @default(autoincrement())
+  name_category String
+  product       Product?
 }
```


